{% extends 'base.html.twig' %}

{% block title %}Hello!{% endblock %}

{% block body %}

    <div class="example-wrapper">

        <h1>User create page! ✅</h1>

        <div>
            {{ form_start(user_form) }}
            {{ form_row(user_form.username) }}
            {{ form_row(user_form.email) }}
            {{ form_row(user_form.plainPassword.first) }}
            {{ form_row(user_form.plainPassword.second) }}
            <div class="gap"></div>
            <button type="submit" method="POST" class="btn">Register!</button>
            {{ form_end(user_form) }}
        </div>

    {% endblock %}