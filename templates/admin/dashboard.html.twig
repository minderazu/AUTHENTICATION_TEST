{% extends 'base.html.twig' %}

{% block title %}Hello!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .error {color: red}
</style>

<div class="example-wrapper">

    <div class="row">
        <h1>Administrator page! ✅</h1>
    </div>
    <div class="row">
        <div class="col-4">
            <a href="{{ path('admin_logout') }}">Logout</a>
            <a href="{{ path('create_user') }}">Create User</a>
        </div>
        <div class="col-6">
            <form action = {{ path('user_search') }} method="POST">
                    <label for="email">Email address</label>
                    <input type="email" class="form-control" id="email" name="email" placeholder="Enter user's email">
                    <small id="emailHelp" class="form-text text-muted">Search the users database</small>
                    <button type="search" class="btn btn-primary">Search</button> 
            </form>
            
        </div>
    </div>
</div>

{% endblock %}